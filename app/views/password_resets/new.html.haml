%h1 Восстановление пароля

= form_tag password_resets_path, :method => :post do
  .field
    = label_tag :email, 'Электропочта'
    = text_field_tag :email, params[:email]
  .actions= submit_tag "Сбросить пароль"
