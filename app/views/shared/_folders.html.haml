- folders = current_user.folders.reserved
- unless folders.empty?
  .my_folders
    %p You have reserved folders:
    - folders.each do |folder|
      %p= link_to folder.commission.name, "/protocols/unfold/#{folder.id}",
        :method => :post
