- if login?
  %li
    = link_to('Выйти', session_path(session.id), :method=>'delete')
    = link_to('Править', edit_user_path(current_user))
- else
  %li
    = link_to('Войти', new_session_path)
-# if user_signed_in?
  -#%li
    -#= link_to('Edit account', edit_registration_path)
-# else
  -#%li
    -#= link_to('Sign up', new_user_registration_path)
